import styled from '@emotion/styled';

const Separator = styled.hr`
  border: none;
  margin: 4px;
`;

const VisibleSeparator = styled.hr`
  width: 100%;
  border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  margin-bottom: 20px;
`;

export { Separator, VisibleSeparator };
